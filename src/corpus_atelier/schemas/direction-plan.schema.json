{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "directions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 3,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "label": {"type": "string", "minLength": 1},
          "concept": {"type": "string", "minLength": 1},
          "primary_variation_axes": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "enum": ["concept", "composition", "palette", "typography", "image_making", "movement"]
            }
          },
          "variation_plan": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "composition": {"type": "string", "minLength": 1},
              "palette": {"type": "string", "minLength": 1},
              "typography": {"type": "string", "minLength": 1},
              "image_making": {"type": "string", "minLength": 1}
            },
            "required": ["composition", "palette", "typography", "image_making"]
          },
          "movement_references": {
            "type": "array",
            "items": {"type": "string"}
          },
          "contrast_statement": {"type": "string", "minLength": 1}
        },
        "required": ["label", "concept", "primary_variation_axes", "variation_plan", "movement_references", "contrast_statement"]
      }
    }
  },
  "required": ["directions"]
}
