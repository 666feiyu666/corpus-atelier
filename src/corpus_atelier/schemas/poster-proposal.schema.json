{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "candidate_id": {"type": "string", "pattern": "^c0[1-3]$"},
    "brief_interpretation": {"type": "string", "minLength": 1},
    "chosen_direction": {"type": "string", "minLength": 1},
    "design_description": {"type": "string", "minLength": 1, "description": "A complete provider-independent description from which a reader can mentally reconstruct the finished image: subjects, relationships, composition, hierarchy, typography, palette, medium, lighting, material, texture, and degree of realism."},
    "design_rationale": {"type": "string", "minLength": 1, "description": "Concise, human-reviewable explanation of why the design decisions answer the brief."},
    "review_criteria": {"type": "array", "items": {"type": "string", "minLength": 1}}
  },
  "required": ["candidate_id", "brief_interpretation", "chosen_direction", "design_description", "design_rationale", "review_criteria"]
}
