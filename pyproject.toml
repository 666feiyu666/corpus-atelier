[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "corpus-atelier"
version = "0.3.0"
description = "Reproducible graphic-design experiments with optional corpus grounding"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1,<2",
    "Pillow>=10,<13",
    "openai>=2,<3",
    "jsonschema>=4,<5",
    "langgraph>=1,<2",
    "streamlit>=1.40,<2",
]

[project.scripts]
corpus-atelier = "corpus_atelier.cli:main"

[tool.setuptools]
packages = {find = {where = ["src"]}}

[tool.setuptools.package-dir]
"" = "src"
[tool.setuptools.package-data]
corpus_atelier = ["prompts/**/*.md", "schemas/*.json"]
