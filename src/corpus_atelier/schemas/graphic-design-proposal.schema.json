{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "candidate_id": {"type": "string", "pattern": "^c0[1-3]$"},
    "status": {"type": "string", "enum": ["ready", "needs_sources", "needs_clarification"], "description": "Ready only when both unresolved arrays are empty and the design description is complete."},
    "brief_interpretation": {"type": "string"},
    "chosen_direction": {"type": "string"},
    "design_description": {"type": "string", "description": "A complete provider-independent description from which a reader can mentally reconstruct the finished image: subjects, relationships, composition, hierarchy, typography, palette, medium, lighting, material, texture, and degree of realism."},
    "design_rationale": {"type": "string", "description": "Concise, human-reviewable explanation of why the design decisions answer the brief."},
    "review_criteria": {"type": "array", "items": {"type": "string"}},
    "source_requirements": {"type": "array", "items": {"type": "string"}},
    "clarification_questions": {"type": "array", "items": {"type": "string"}}
  },
  "required": ["candidate_id", "status", "brief_interpretation", "chosen_direction", "design_description", "design_rationale", "review_criteria", "source_requirements", "clarification_questions"]
}
